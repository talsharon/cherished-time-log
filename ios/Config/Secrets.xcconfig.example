// Copy to Secrets.xcconfig (gitignored). Only set host + anon key — no https:// on one line.

COLON = :
SLASH = /
SUPABASE_HOST = YOUR_PROJECT_REF.supabase.co
SUPABASE_URL = https$(COLON)$(SLASH)$(SLASH)$(SUPABASE_HOST)
SUPABASE_ANON_KEY = your_anon_public_key_here
